# Pinned to what the recorded measurement was actually taken with, so the number can be reproduced
# rather than approximated. Unlike the GPTCache harness next door, nothing here is held back: the
# reference verifier runs on current releases.
sentence-transformers==5.6.1
transformers==5.14.1
torch==2.13.0
numpy==2.4.6
